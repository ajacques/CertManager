%h1= t 'views.agents.new.header'
%noscript JavaScript is required for this page to function.
- unless @signing_key
  .bg-danger= t 'views.agents.new.errors.no_signing_key'
%div
  %div= t 'views.agents.new.sub_header'
  %ol
    %li
      %h4 Firewall/networking
      %div= t 'views.agents.new.network_step.message', port: 443
      %code Container -> #{root_url}
    = react_component 'AgentNewForm', { imageName: @agent.image_name }, prerender: true, data: { react_mount: true, initialToken: 'foo' }
