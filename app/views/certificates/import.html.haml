%h2 Import from another server
%p This will import an entire certificate chain from an existing public website. The private key will have to be imported manually
= form_tag action: :import_from_url do
  = text_field_tag :host
  = submit_tag 'Import'
%div.drop-target{data: {drop: {type: :textbox, target: :key}}}
  = bootstrap_form_for :certificate, url: url_for(action: :import) do |f|
    = f.text_area :key, label: (t 'certificate'), placeholder: "Insert a valid certificate key pair\r\n-----BEGIN CERTIFICATE-----", class: :certificate
    = f.submit 'Import certificate', class: 'btn btn-primary'