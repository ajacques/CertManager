%h1= @cert.subject
.bg-warning
  %p This certificate is a stub and does not contain any public or private key data. As such no detailed data can be shown
  %p Please import the key to view more data.
  %p The only thing we know about the certificate is the subject:
= render partial: 'subject', locals: { subject: @cert.subject }
