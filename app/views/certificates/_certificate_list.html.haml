%table.table.table-hover.table-condensed
  %thead
    %tr
      %td Common Name
      %td Status
      %td Expiration
  %tbody
    = render partial: 'certificate_row', collection: @expiring, as: :cert
    = render partial: 'certificate_row', collection: @certs, as: :cert
- if @certs.respond_to? :page
  .row
    .center-block.col-md-3= will_paginate @certs